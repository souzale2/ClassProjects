@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<Team>;

<h4>@ViewBag.InViewData</h4>
<h4>@ViewData["InViewBag"]</h4>
<h4>@TempData["InTempData"]</h4>
<h4>@TempData["InTempData"]</h4>
</br>
keys
@ViewData["life"]; ????? what happens?

@ViewBag.Life.ToString()

</br>
values
@ViewData.Values

<div class="col-sm-9">
    <ul class="list-inline">
        @foreach (Team team in ViewBag.Teams)
        {
            <li class="list-inline-item">
                <a asp-action="Details" asp-route-id="@Model.">
                    <img src="~/images/@team.LogoImage" alt="@team.Name"
                         title="@team.Name " />
                </a>
            </li>
        }
    </ul>
</div>